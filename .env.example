# Copia este archivo como `.env` y reemplaza con tus claves reales.
# NUNCA pegues claves reales en este archivo: se versiona en git.

# OBLIGATORIA: clave de Google AI Studio para Gemini.
GEMINI_API_KEY=tu_clave_de_gemini_aqui

# RECOMENDADA: clave de NVD. Sin ella el rate limit es 5 req/30s.
# https://nvd.nist.gov/developers/request-an-api-key
NVD_API_KEY=tu_clave_de_nvd_aqui

# OPCIONAL: ruta al inventario por defecto (la UI de upload tiene prioridad).
# INVENTARIO_PATH=uploads/inventario.xlsx
